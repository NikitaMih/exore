import { Flex, Title } from '@mantine/core';

export const NotFound = () => {
  return (
    <Flex m={270} align={'center'} justify={'center'}>
      <Title size={60} c={'blue'}>
        404
      </Title>
    </Flex>
  );
};
